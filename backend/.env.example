# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your-super-secret-jwt-key

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# Cloudflare Turnstile (server-side verification for protected auth actions)
TURNSTILE_SECRET=your_turnstile_secret
TURNSTILE_HOSTNAMES=streetplate.co.za,www.streetplate.co.za

# Shared Redis rate limiter. Required in every deployed environment so limits
# remain consistent across Railway replicas and restarts.
KV_REST_API_URL=https://your-redis-endpoint.upstash.io
KV_REST_API_TOKEN=your_redis_rest_token
RATE_LIMIT_NAMESPACE=streetplate-production

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Private ClamAV service used to scan every image before Cloudinary upload.
# Production defaults to fail-closed scanning; set MALWARE_SCAN_ENABLED=false
# only for local development without ClamAV. Never expose clamd publicly.
MALWARE_SCAN_ENABLED=false
CLAMAV_HOST=clamav.railway.internal
CLAMAV_PORT=3310
CLAMAV_TIMEOUT_MS=10000

# PayFast
PAYFAST_MERCHANT_ID=your_merchant_id
PAYFAST_MERCHANT_KEY=your_merchant_key
PAYFAST_PASSPHRASE=your_payfast_passphrase
PAYFAST_SANDBOX=true
PAYFAST_DEBUG=false
PAYFAST_ATOMIC_RPC_ENABLED=false
PAYFAST_VALIDATION_TIMEOUT_MS=10000
# Optional override; defaults to PayFast's documented hosts.
PAYFAST_VALIDATION_HOSTS=

# App
# Production PayFast callbacks require a public HTTPS backend URL.
API_URL=https://api.streetplate.co.za
WEB_APP_URL=https://streetplate.co.za
# Number of trusted reverse-proxy hops in production (verify for your host).
TRUST_PROXY_HOPS=1

# Resend payment confirmation email
RESEND_API_KEY=
RESEND_ORDERS_FROM=StreetPlate Orders <orders@streetplate.co.za>
RESEND_TIMEOUT_MS=5000

# Driver dispatch
DRIVER_OFFER_TIMEOUT_SEC=30
DRIVER_PAYOUT_SHARE=0.80
MAX_DRIVER_SEARCH_RADIUS_KM=10
OSRM_BASE_URL=https://router.project-osrm.org/route/v1
EXPO_ACCESS_TOKEN=
